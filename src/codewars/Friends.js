/**
 * MY SOLUTION IS ALSO THE BEST
 * Gets array with names that have exactly 4 chars
 * @param {array} friends
 * @return {array} filtered array
 */
export function friend(friends) {
  return friends.filter(name => name.length === 4);
}

// console.log(friend(result.first));

